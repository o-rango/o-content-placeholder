pipeline {
  agent any
  stages {
    stage('Lint') {
      steps {
        sh 'npm install -g lerna'
      }
    }
    stage('Build Merchant Android') {
      steps {
        sh 'npm install '
      }
    }
    stage('') {
      steps {
        sh 'npm install ionic'
      }
    }
  }
}